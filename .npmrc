# Allow install scripts for packages that require them at build time.
# sharp  — native image processing (used by Next.js Image Optimisation)
# unrs-resolver — Rust-based module resolver used by ESLint / Next.js tooling
allow-scripts=pkg:npm/sharp@*,pkg:npm/unrs-resolver@*
